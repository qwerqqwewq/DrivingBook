<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pj19980729.drivingbook">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.fullscreen" />
        <activity android:name=".sign.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sign.RegistActivity" />
        <activity android:name=".emptyactivity.FirstActivity" />
        <activity android:name=".emptyactivity.SecondActivity" />
        <activity android:name=".emptyactivity.ThirdActivity" />
        <activity android:name=".emptyactivity.ForthActivity" />
        <activity android:name=".test.TestActivity" />
        <activity android:name=".test.ExamActivity" />
        <activity android:name=".test.WrongActivity" />
        <activity android:name=".emptyactivity.ViewPlayerActivity" />
        <activity android:name=".test.specialActivity" />
        <activity android:name=".test.specialtestActivity" />
        <activity android:name=".LoveCommentActivity" />
        <activity android:name=".LoveActivity"></activity>
    </application>

</manifest>